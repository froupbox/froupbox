{
	"name": "froupbox",
	"version": "1.0",
	"description": "froupbox is a mod of various BeepBox mods that aims to advance their capabilities.",
	"author": "Pastenkopie",
	"license": "MIT",
	"homepage": "https://froupbox.github.io/",
	"type": "module",
	"main": "main.js",
	"icon": "path.join(__dirname, 'icon.ico')",
	"scripts": {
		"build": "rsbuild build",
		"dev": "rsbuild dev",
		"preview": "rsbuild preview",

		"build-all": "rsbuild build",
		"build-synth": "rsbuild build",
		"build-player": "rsbuild build",
		"build-editor": "rsbuild build",
		"build-offline": "rsbuild build",
		"build-offline-html": "echo 'froupbox does not support offline html (due to using rust). sorry :('",
		"live_editor": "rsbuild dev",
		"let": "rsbuild dev",

		"deploy-files": "echo 'unimplemented for now. :('",
		"start": "echo 'unimplemented for now. :('",
		"package": "echo 'unimplemented for now. :('",
		"package-host": "echo 'unimplemented for now. :('"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/froupbox/froupbox.git"
	},
	"devDependencies": {
		"@electron/packager": "^18.0.0",
		"electron": "^33.0.0"
	},
	"dependencies": {
		"@rsbuild/core": "^2.0.15",
		"@types/jquery": "^3.5.30",
		"@types/select2": "^4.0.63",
		"imperative-html": "^0.1.1",
		"libflacjs": "^5.6.0",
		"js-xxhash": "^4.0.0"
	}
}
